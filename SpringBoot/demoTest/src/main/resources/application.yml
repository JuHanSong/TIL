spring:
  datasource:
    url: jdbc:mariadb://172.30.1.12:3333/test
    driver-class-name: org.mariadb.jdbc.Driver
    data-username: "root"
    data-password: "0000"

  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      dal-auto: update

server:
  port: 8888